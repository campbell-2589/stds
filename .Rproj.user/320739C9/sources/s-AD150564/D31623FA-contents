# On Model Averaging 

Recall that we can break down model error into the bias an variance. $bias(\hat{Y})= E[\hat{Y}-E[Y]]$

$\operatorname{MSE}\left(\hat{Y}_{m}\right)=\left\{\operatorname{bias}\left(\hat{Y}_{m}\right)\right\}^{2}+\operatorname{var}\left(\hat{Y}_{m}\right)$